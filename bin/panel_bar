#! /bin/sh
#
# Example panel for LemonBoy's bar

occupied="◼"
unoccupied="◻"

#occupied="="
#unoccupied="-"

focuscolor="%{F#9EB0C6}"
unfocuscolor="%{F#707F93}"
backcolor="%{B#4A576A}"

while read -r line ; do
    case $line in
        C*)
            # conky yall
            conky="$focuscolor${line#?}"
            ;;
        W*)
            # bspwm internal state
            wm_infos=""
            IFS=':'
            set -- ${line#?}
            while [ $# -gt 0 ] ; do
                item=$1
                name=${item#?}
                case $item in
                    O*)
                        # focused occupied desktop
                        wm_infos="$wm_infos $focuscolor$backcolor$occupied${name}"
                        ;;
                    F*)
                        # focused free desktop
                        wm_infos="$wm_infos $focuscolor$backcolor$unoccupied${name}"
                        ;;
                    U*)
                        # focused urgent desktop
                        wm_infos="$wm_infos $focuscolor$backcolor$occupied${name}"
                        ;;
                    o*)
                        # occupied desktop
                        wm_infos="$wm_infos $unfocuscolor$backcolor$occupied${name}"
                        ;;
                    f*)
                        # free desktop
                        wm_infos="$wm_infos $unfocuscolor$backcolor$unoccupied${name}"
                        ;;
                    u*)
                        # urgent desktop
                        wm_infos="$wm_infos $unfocuscolor$backcolor$occupied${name}"
                        ;;
                esac
                shift
            done
            ;;
    esac
    printf "%s\n" "%{l}$wm_infos %{c} %{r}$conky%{A:sh /home/esmth/bin/lock:} %{A}"
done
